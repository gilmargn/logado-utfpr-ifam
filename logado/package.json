{
  "name": "LOGADO",
  "displayName": "LOGADO",
  "publisher": "LOGADO",
  "description": "Rastros acadêmicos no aprendizado de programação ",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/gilmargn/logado-utfpr-ifam/"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.108.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:javascript"
  ],
  "main": "./dist/extension.js",
  "contributes": {},
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
 "devDependencies": {
  "@types/vscode": "^1.85.0",
  "@types/node": "^20.x.x",
  "typescript": "^5.x.x"
}
}
